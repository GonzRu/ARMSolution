<Window x:Class="ArmWpfUI.Views.TerminalViews.UpLoadDocumentView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d"
        Title="Загрузка файла" 
        Height="132" Width="377" 
        ResizeMode="NoResize">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Label Content="Загрузка файла. Пожалуйста, полождите..." HorizontalAlignment="Center" FontSize="16" />

        <ProgressBar Grid.Row="1" Height="30" Value="{Binding UploadDocumentProgress}" Maximum="1" Minimum="0" />

        <Button Grid.Row="2" HorizontalAlignment="Right" Content="Отмена" Margin="0, 10, 15, 0" Command="{Binding UploadDocumentAsyncCommand.CancelCommand}"/>
    </Grid>
</Window>
