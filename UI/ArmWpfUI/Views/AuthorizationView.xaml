<Window x:Class="ArmWpfUI.Views.AuthorizationView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Авторизация" Height="193" Width="408" ResizeMode="NoResize">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition />
            <RowDefinition />
        </Grid.RowDefinitions>
        
        <StackPanel Grid.Row="0" Orientation="Horizontal" VerticalAlignment="Center">
            <Label Content="Логин:" Width="100" />
            <TextBox Width="250" Height="30" />
        </StackPanel>

        <StackPanel Grid.Row="1" Orientation="Horizontal" VerticalAlignment="Center">
            <Label Content="Пароль:" Width="100" />
            <TextBox Width="250" Height="30" />
        </StackPanel>

        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            
            <Button Width="150"
                    Height="30"
                    Grid.Column="0"
                    Content="Ок"/>

            <Button Width="150"
                    Height="30"
                    Grid.Column="1"
                    Content="Отмена"/>

        </Grid>

    </Grid>
</Window>
